# home-manager module — currently a placeholder.
#
# v0.1 ships the helper functions via `inputs.hexis.lib` (see
# `./wrap.nix`). The full module — declarative `programs.hexis.managedConfigs`
# option, drift-report viewing helpers, etc. — is deferred until the
# reconciler core lands.
{ ... }: {
}
